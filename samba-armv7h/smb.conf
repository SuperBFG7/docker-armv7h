[global]
   workgroup = WORKGROUP
   server string = RPi3
   printcap name = /dev/null
   load printers = no
   disable spoolss = yes
   log file = /dev/stdout
   max log size = 50
   security = user
   map to guest = Bad User
   dns proxy = no 
   unix extensions = no

[music]
   comment = music
   path = /music/shared
   public = yes
   only guest = yes
   read only = yes
   wide links = yes

[upload]
   comment = upload
   path = /upload
   public = yes
   only guest = yes
   read only = no
   wide links = yes

